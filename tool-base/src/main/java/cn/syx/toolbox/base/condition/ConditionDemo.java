package cn.syx.toolbox.base.condition;

import java.util.ArrayList;

public class ConditionDemo {

    public static void main(String[] args) {
        /*Condition condition = new ConditionBuilder()
                .and("key1", CompareEnum.EQUALS, "value1")
                .or("key2", CompareEnum.GREATER_THAN, 100)
                .and(new ConditionBuilder()
                        .or("key3", CompareEnum.LESS_THAN, 50)
                        .or("key4", CompareEnum.IN, new ArrayList<>())
                        .build())
                .build();

        // 在这里使用条件进行判断
        boolean result = condition.evaluate(new Object());
        System.out.println("Evaluation result: " + result);*/
    }
}
